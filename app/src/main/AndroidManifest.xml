<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DietTracker"
        tools:targetApi="31">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".SignupActivity" android:exported="false" />
        <activity android:name=".LandingActivity" android:exported="false" />
        <activity android:name=".WeightLossActivity" android:exported="false" />
        <activity android:name=".WeightGainActivity" android:exported="false" />
        <activity android:name=".BmiActivity" android:exported="false" />
        <activity android:name=".ProteinFoodsActivity"/>
        <activity android:name=".CarboFoodsActivity"/>
        <activity android:name=".FatFoodsActivity"/>
        <activity android:name=".NutriTrackerActivity"/>
        <activity android:name=".TodayDetailsActivity"/>
        <activity android:name=".HistoryActivity"/>

    </application>

</manifest>
